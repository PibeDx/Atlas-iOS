machine:
  xcode:
    version: 8.2
dependencies:
  pre:
    - gem install bundler
  override:
    - bundle install
    - bundle exec pod install --repo-update
test:
  override: 
    - bundle exec fastlane test
